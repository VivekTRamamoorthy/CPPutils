#include "Matrix.h"
Matrix zeros(int row, int col);
// Matrix zeros(int row, int col){
//     Matrix A(row,col);
//     return A;
    
//     double zerodata[row*col];
//     for (int i = 0; i < row*col; i++)
//     {
//         zerodata[i]=0.0;
//     }
    
//     Matrix newMatrix(zerodata,row,col);
//     return newMatrix;
// };